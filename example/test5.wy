def f() -> int:
    def g(a: int) -> int:
        def h(a: int) -> int:
            return a + 2
        return h(a) + 1
    def g1(a: int) -> int:
        return a + 1
    return g(4) + g1(5)
print(f())